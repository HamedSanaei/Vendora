/**
 * Layout shim: every page imports the footer from here, so the whole
 * storefront now renders the Penpot-based Vendora footer.
 */
export { StoreFooter as default } from "@components/vendora/layout/store-footer";
